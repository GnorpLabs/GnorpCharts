apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "SPT-FIKA.fullname" . }}-test-connection"
  labels:
    {{- include "SPT-FIKA.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "SPT-FIKA.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
